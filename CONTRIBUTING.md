# Issue 提交规范

感谢你对本 Mod 的关注与贡献！为了帮助我们更高效地处理问题，请在提交 Issue 前仔细阅读以下规范。

## 提交前必做检查

1. **搜索已有 Issue**  
   请先使用 GitHub 的搜索功能，确认是否已有相同或类似的问题，避免重复提交。

2. **确认问题是否由本 Mod 引起**  
   - 请先尝试**只安装本 Mod + 对应加载器**，排除其他模组冲突。
   - 确认问题在最新版本中是否仍然存在。

3. **准备必要信息**  
   提交 Bug 报告时，请务必附上下方要求的完整信息，否则可能会被要求补充或关闭。

---

## 适用的 Issue 类型

| 类型       | 说明                           | 建议标签          |
| ---------- | ------------------------------ | ----------------- |
| Bug 报告   | 游戏崩溃、功能异常、错误行为等 | `bug`             |
| 功能建议   | 希望增加的新功能或改进         | `enhancement`     |
| 兼容性问题 | 与其他 Mod / 资源包冲突        | `compatibility`   |
| 性能问题   | 卡顿、帧率下降、内存占用过高等 | `performance`     |
| 文档问题   | Wiki / README 描述不清晰或错误 | `documentation`   |
| 其他       | 不属于以上分类的内容           | `question` 或其他 |

---

## Bug 报告规范

请使用以下模板，或直接复制填写：

```markdown
### 问题描述
简洁清晰地描述遇到的问题。

### 复现步骤
1. （如刚启动游戏就崩溃）
2. （如放置了某个方块后多长时间崩溃了）
3. 
...

### 预期行为
你期望发生什么。

### 实际行为
实际发生了什么（可附截图或视频，或写见错误报告）。

### 环境信息
- **Minecraft 版本**：1.20.1 / 1.21.1 / 26.2 等
- **加载器及版本**：Fabric Loader x.xx.x 
- **本 Mod 版本**：例如 1.2.3
- **其他已安装的 Mod**：（如有冲突嫌疑，请列出主要相关模组，或附上完整 mod 列表）
- **操作系统**：Windows / Linux / macOS
- **Java 版本**

### 日志与崩溃报告
- 请附上本模组 `config/simplevillager/` 中的 `client.toml` 和 `server.toml` 文件
- 请附上 `logs/latest.log`
- **请直接附上导致游戏崩溃的mod的文件（你正在用的那个）**
- 如果发生崩溃，请附上启动器如 PCL 或 HMCL 导出的zip日志报告
- 日志请使用 [GitHub Gist](https://gist.github.com/) 或直接粘贴（过长时建议用 Gist 或直接附文件或压缩包）

### 附加信息
其他你认为有帮助的信息（截图、视频链接、相关讨论等）。